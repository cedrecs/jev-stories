node_modules/
.wrangler/

# Secrets: never commit these. The API key lives only in .dev.vars locally
# and as a Cloudflare secret in production.
.dev.vars
.dev.vars.*
!.dev.vars.example
.env
.env.*
*.pem
*.key

# Personal Claude Code settings (permissions, local paths)
.claude/settings.local.json

*.log
.DS_Store
